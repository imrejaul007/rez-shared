name: Payment Service CI

on:
  push:
    branches: [main, develop, 'feature/**', 'fix/**']
    paths: ['rez-payment-service/**', 'rez-shared/**']
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: '20'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    services:
      mongodb:
        image: mongo:7
        ports: ['27017:27017']
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ping:1})'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports: ['6379:6379']
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: rez-payment-service/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: rez-payment-service
      - name: Type check
        run: npx tsc --noEmit
        working-directory: rez-payment-service
        continue-on-error: true
      - name: Lint
        run: npm run lint
        working-directory: rez-payment-service
        continue-on-error: true
      - name: Run tests
        run: npm test -- --forceExit --passWithNoTests
        working-directory: rez-payment-service
        env:
          NODE_ENV: test
          MONGODB_URI: mongodb://localhost:27017/rez-test-payment
          REDIS_URL: redis://localhost:6379
          JWT_SECRET: ci-jwt-secret-min-32-characters-long
          JWT_ADMIN_SECRET: ci-jwt-admin-secret-32chars-long
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
          RAZORPAY_KEY_ID: test_key
          RAZORPAY_KEY_SECRET: test_secret
          RAZORPAY_WEBHOOK_SECRET: test_webhook_secret
          OTEL_ENABLED: 'false'

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: rez-payment-service/package-lock.json
      - name: Install and build
        run: npm ci && npm run build
        working-directory: rez-payment-service

  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '${{ env.NODE_VERSION }}'
      - name: Audit dependencies
        run: npm audit --audit-level=high
        working-directory: rez-payment-service
        continue-on-error: true
