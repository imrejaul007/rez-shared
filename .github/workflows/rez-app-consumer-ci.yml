name: Consumer App CI

on:
  push:
    branches: [main, develop, 'feature/**', 'fix/**']
    paths: ['rez-app-consumer/**', 'rez-shared/**']
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: '20'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: rez-app-consumer/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: rez-app-consumer
      - name: Type check
        run: npx tsc --noEmit
        working-directory: rez-app-consumer
        continue-on-error: true
      - name: Lint
        run: npm run lint
        working-directory: rez-app-consumer
        continue-on-error: true
      - name: Run unit tests
        run: npm test -- --passWithNoTests
        working-directory: rez-app-consumer
        env:
          NODE_ENV: test
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: consumer-app-coverage
          path: rez-app-consumer/coverage/
          retention-days: 7

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: rez-app-consumer/package-lock.json
      - name: Install and build
        run: npm ci && npm run build:render
        working-directory: rez-app-consumer

  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '${{ env.NODE_VERSION }}'
      - name: Audit dependencies
        run: npm audit --audit-level=high
        working-directory: rez-app-consumer
        continue-on-error: true
